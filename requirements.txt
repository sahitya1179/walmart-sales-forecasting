# Core
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12

# Time-series models
statsmodels>=0.14
pmdarima>=2.0
prophet>=1.1

# Neural network (ANN)
# TensorFlow is optional: if it is unavailable (e.g. no wheel for your Python
# version yet), the ANN model automatically falls back to scikit-learn's
# MLPRegressor, so the pipeline still runs. Uncomment to use a Keras MLP.
# tensorflow>=2.13

# Notebook
jupyter>=1.0
nbconvert>=7.0
ipykernel>=6.0
